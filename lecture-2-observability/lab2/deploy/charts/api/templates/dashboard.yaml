{{- if .Values.dashboard.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-red-dashboard
  labels:
    grafana_dashboard: "1"
data:
  api-red.json: |-
{{ .Files.Get "dashboards/api-red.json" | indent 4 }}
{{- end }}
